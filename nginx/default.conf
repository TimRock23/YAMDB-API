server {
    listen 80;

    location /static/ {
        root /var/www/;
    }

    location /media/ {
        root /var/www/;
    }

    location / {
        include proxy_params;
        proxy_pass http://web:8000;
    }

    server_tokens off;
}